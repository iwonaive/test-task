import React from "react";
import "./ResultsNumber.css";

const ResultsNumber: React.FC = () => {
  return <div className="results-number">Liczba wyników: 23</div>;
};

export default ResultsNumber;

// here I should add later: type: number, const ResultsNumber { count } and show in return Liczba wyników: {count}